name: CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Descargar el código
        uses: actions/checkout@v4

      - name: Iniciar el cluster de contenedores
        run: |
          docker compose up -d && echo "¡Cluster iniciado con éxito!"
